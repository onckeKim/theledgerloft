# Decision Log

Format: ID · date · decision · reason · status.

| ID | Date | Decision | Reason | Status |
|---|---|---|---|---|
| D-001 | 2026-09-24 | Follow the Claude Build Playbook (App 3: Ledger Loft) one step at a time, with a quality gate after each phase | Keeps changes small and verifiable | Accepted |
| D-002 | 2026-09-24 | MVP uses manual entry only, with no bank integrations | Lower security and regulatory risk; faster to validate | Accepted |
| D-003 | 2026-09-24 | Product is positioned as a budgeting and organisation tool, not financial advice | Avoids regulated-advice obligations | Accepted |
| D-004 | 2026-09-24 | Stack is Next.js + TypeScript, Supabase, Tailwind, Vercel, PayFast sandbox | Playbook baseline; to be confirmed in A3 | Proposed |
| D-005 | 2026-09-24 | Money is stored as integer minor units (cents), ZAR by default | Avoids floating-point rounding errors | Proposed |
| D-006 | 2026-09-24 | Primary MVP audience is existing and prospective planner buyers who already want to budget; people in acute debt distress are signposted to regulated help, not served | Keeps the product inside the organisation/education boundary | Proposed (confirm after interviews) |
| D-007 | 2026-09-24 | Debt features show snowball and avalanche as user-chosen orderings labelled "Estimate", never as a recommendation | Avoids advice and debt-counselling territory | Accepted |
| D-008 | 2026-09-24 | Pilot is a paid, capped founding offer using a once-off PayFast payment; prices set by owner | Paying pilots give real demand signal; once-off avoids early subscription integration | Proposed |
| D-009 | 2026-09-24 | App uses The Ledger Loft Co store design tokens unchanged; `design/tokens/tokens.json` is the source of truth, checked by `scripts/check-tokens.mjs` | App, store and planners read as one brand | Accepted |
| D-010 | 2026-09-24 | Add an app-only layer (`design/tokens/app.css`) with darker sage/gold/warning text tones, a navy focus ring and inkMuted control borders | Several store pairings fail WCAG 2.2 AA for small text and form controls. Approved 2026-09-24: `sageText`, `goldText`, `warningText` added to the brand tokens and the light `label` switched to `sageText` | Accepted |
| D-011 | 2026-09-24 | Money format is `R 1 234,56` (en-ZA: non-breaking space thousands separator, comma decimal, real minus sign) in the app and exports | Owner choice; matches the local convention | Accepted |
| D-012 | 2026-09-24 | Money is integer cents and rates are integer basis points; rounding happens only in the four places listed in the L4 spec (percent down, bar permille down, monthly interest half away from zero, needed-per-month up) | Exact, repeatable numbers across app, exports and tests | Accepted |
| D-013 | 2026-09-24 | Budget period is labelled by the month it ends in; month start day limited to 1–28 ("last working day" out of MVP) | Payday budgeting reads naturally; every month has the day | Accepted |
| D-014 | 2026-09-24 | Debt projection: fixed order per run, monthly interest then minimums then roll-over, 600-month cap, always labelled Estimate; user picks snowball or avalanche with no default recommendation | Simple, explainable, inside the L1 advice boundary | Accepted |
| D-015 | 2026-09-24 | MVP assumptions A-01…A-07 adopted as defaults (Etsy buyers first, single-person households, monthly period with 4 pay frequencies, PDF + CSV export, accounts created at build time, POPIA-aware region choice, English only) | Owner said go before answering; conservative defaults keep scope small and reversible | Proposed (owner to confirm) |
| D-016 | 2026-09-24 | Analytics never receive amounts, free text or names; no session replay in the app | Financial data is C3; keeps the trust promise | Accepted |
| D-017 | 2026-09-24 | PRD decisions P-1…P-7: sign up then pay; no manual accounts or transfers in MVP; goal, sinking fund and debt actions create matching budget transactions; debt interest never added automatically (user updates balance); new periods copy the last plan; categories with history are archived, not deleted | Keeps one consistent money model across budget, goals and debts; smallest MVP | Accepted |
| D-018 | 2026-09-24 | App stack as generated by create-next-app 16.3.6: Next.js 16.3, React 19.2, TypeScript 5, Tailwind 4, ESLint 9 (flat config) + Prettier, Vitest 5 + Testing Library, Playwright 1.56 + axe-core; Zod 4 for env validation; Lucide icons. Tailwind's default palette, fonts and type scale are removed so only brand tokens can be used | Current stable versions with a known-compatible set; tokens enforced by the build, not by memory | Accepted |
| D-019 | 2026-09-24 | Static security headers now; nonce-based Content-Security-Policy added in A4 via `proxy.ts` | Nonce CSP needs per-request rendering, which signed-in pages get with auth; avoids forcing marketing pages dynamic | Implemented in A4: every page renders per request (root layout `connection()`) so all pages get the nonce; `style-src` allows inline styles |
| D-020 | 2026-09-24 | `APP_PREVIEW=synthetic` lets developers view signed-in screens before auth exists; env validation refuses it in production; removed in A4 | Lets UI work proceed without weakening production protection | Superseded 2026-09-24 by Supabase Auth (A4); removed |
| D-021 | 2026-09-24 | Auth checks live in a data access layer (`verifySession()` in every page and data function), not in layouts; a unit test enforces it | Next.js 16 guidance: layouts don't re-run on navigation | Accepted |
| D-022 | 2026-09-24 | Supabase project `theledgerloft` in **eu-west-2 (London)**, free plan, in the owner's existing organisation | No African region is offered; London is among the nearest and UK data-protection law is comparable to POPIA (cross-border basis still needs legal confirmation, A-06). Free plan limits backups and pauses idle projects, so revisit before the pilot (N12) | Accepted |
| D-023 | 2026-09-24 | Schema choices: goals and sinking funds share one `goals` table (`kind`); `expense_items` not created (budget lines per period cover it, PRD P-6); composite `(id, household_id)` foreign keys on every cross-table reference; `created_by` is an audit column without FK; archive (`archived_at`) for user-facing removal, soft delete (`deleted_at`) only for transactions (undo) | Fewer tables to secure; database-level guarantee against cross-household links | Accepted |
| D-024 | 2026-09-24 | Auth: Supabase Auth email + password with email confirmation; `proxy.ts` refreshes sessions; pages and data verified with `getClaims()`; no service-role key in the app; sign-up provisions profile, household and owner membership in a database trigger | Supabase SSR guidance; least privilege | Accepted |
| D-025 | 2026-09-24 | Setup saves go through one Postgres function per step (security invoker, household from `auth.uid()`, refuses after completion, per-household advisory lock); the browser autosaves only when the whole step is valid and queues saves one at a time | Atomic, can't touch another household, can't lose data to half-typed rows or duplicate rows from a blur-then-click race | Accepted |
| D-026 | 2026-09-24 | Local development and CI use the Supabase CLI stack in Docker (`npm run db:start`); images pulled from Docker Hub where the default registry is blocked (`SUPABASE_INTERNAL_IMAGE_REGISTRY=docker.io`) | Real Postgres + Auth for SQL and end-to-end tests without touching the London project | Accepted |
| D-027 | 2026-09-24 | Months are created on demand by `ensure_budget` (copying the most recent earlier plan, skipping archived categories), only from the first month up to next month; budget changes go through security-invoker functions with a per-household lock | A plan exists whenever the user opens a month (P-6) without inventing history or far-future months | Accepted |
| D-028 | 2026-09-24 | The budget page uses `?period=YYYY-MM` rather than the `/app/budget/[period]` route in the PRD route map, and planned amounts edit inline on phones too (rows stack) rather than in a sheet | One page, shareable URL either way; a sheet adds a dialog to make accessible for little gain. Revisit after pilot feedback | Proposed (owner to confirm) |
| D-029 | 2026-09-24 | Deleting an auth user deletes every household where they are the only member (database trigger); shared households are kept for the remaining members | No orphaned C3 data (N11, T15) and ready for the US-44 self-service deletion | Accepted |
| D-030 | 2026-09-24 | A debt's current balance is stored (`debts.balance_cents`) and changed only by `debt_record_payment` and `debt_set_balance`, which also write the history row; goal "saved" stays computed from contributions (L4 §8) | A statement update resets the balance, so replaying history can't reproduce it; one locked function call keeps the stored balance and history consistent | Accepted |
| D-031 | 2026-09-24 | Transactions written by a goal or debt are changed only through that goal or debt (trigger `guard_linked`); app-managed categories are hidden from the add-transaction picker; adding a goal or debt adds this month's plan line only if it's missing, and changing the plan to match is a separate, explicit button | Saved amounts, balances and budget actuals can't drift apart; the plan changes only when the user changes it | Accepted |
| D-032 | 2026-09-24 | Goals and debts get their own pages (`/app/goals/new`, `/app/goals/[id]`, `/app/debts/new`, `/app/debts/[id]`) instead of dialogs; the payoff method toggle is a plain form post and "explore paying extra" is a `?extra=` query that's never saved | Pages are simpler to make accessible and work before JavaScript loads; exploring doesn't change the plan | Proposed (owner to confirm) |
| D-033 | 2026-09-24 | Exports are built when downloaded and never stored: creating one writes an `exports` row (audited) whose link works for 10 minutes, and `GET /api/exports/[id]` streams the file to its own household only. No Storage bucket, `storage_path` stays empty, and no numbers are snapshotted in the database (amends L4 §8). At most 20 exports an hour per household | Meets US-39 AC4 more strictly (nothing left to delete after 24 hours) and keeps C3 data out of file storage | Accepted |
| D-034 | 2026-09-24 | A monthly review reports what was planned and recorded and never creates a plan. Planned income in a review is today's income items, because income isn't stored per month yet | Honest numbers without inventing history; per-month income can come later if pilots need it | Proposed (owner to confirm) |
| D-035 | 2026-09-24 | PDFs use pdf-lib with Inter and Playfair Display embedded (SIL OFL, licence files kept next to the fonts). Inter is embedded whole because pdf-lib's subsetting dropped its glyphs; files are about 360 KB. The PDF isn't tagged for screen readers; the same content is on the accessible review page | Brand fonts with the non-breaking space and minus sign the money format needs; a known library limitation | Accepted |
| D-036 | 2026-09-24 | The notify route applies payments through `public.payfast_apply_itn`, a database function gated by a server-held secret (bcrypt hash in `private.app_secrets`), instead of a service-role key. Privileged bodies live in the unexposed `private` schema behind SECURITY INVOKER wrappers; only anon (no session) may call it | Keeps the rule "no service-role key in the app": the secret can do exactly one thing, and the function still checks amount, merchant and idempotency itself | Accepted |
| D-037 | 2026-09-24 | The pilot price and length live in `private.plans`, set by the owner with SQL; the plan is closed (no price) until then, and `/app/join` says so. Pilot access is required for every planner page and action from L9 on; settings, data export and joining aren't gated | "Price from server config" (US-06 AC2) that the browser can't touch and the database can enforce; payment is the MVP's access rule (P-1) | Accepted |
| D-038 | 2026-09-24 | PayFast integration rules are taken from PayFast's official PHP SDK (commit 015efcd, Feb 2024) because developers.payfast.co.za is blocked in the build environment: checkout signature, notification signature, the 4 PayFast host names, and server confirmation at `/eng/query/validate`. The notification's source address comes from the hosting platform's `x-real-ip` / `x-forwarded-for`, which Vercel sets (another host must do the same). The owner re-checks against the current docs and runs a real sandbox payment before going live | Follows official PayFast code, not memory; states what couldn't be verified here | Proposed (owner to confirm before live) |
| D-039 | 2026-09-24 | Growth funnel rules: an Etsy purchase never needs the app; Etsy buyers are never added to lists from order data (opt-in only); the lead magnet lives on our own site; nothing Etsy-facing is built until `docs/l10/etsy-policy-questions.md` is answered from Etsy's official pages; no emails or prompts triggered by money events or near the debt-help card | Protects buyers and the shop's standing; Etsy's pages couldn't be read from the build environment, so no policy is assumed | Accepted |
| D-040 | 2026-09-24 | Release candidate review: **no-go** for a pilot with real people or money until blockers B1–B9 in `docs/release/rc-review-2026-09-24.md` are cleared (deployment, a real PayFast sandbox payment, account deletion and the other Must stories, legal, backups, monitoring and support, interviews, CI on GitHub) | The product is well tested locally, but launch evidence is missing (playbook: don't declare production-ready without evidence) | Accepted |
| D-041 | 2026-09-24 | Budget months follow the budgets that exist: a date covered by a budget belongs to it; only uncovered dates use the start-day rule. A start-day change keeps this month, makes next month a one-off transition month (day after this month ends → end of next month under the new day), and moves later budgets and future-dated transactions to match (L4 §3.1) | Meets US-41 AC1 ("applies from the next period") with no gaps, overlaps or moved history; the L4 vectors are unchanged | Accepted |
| D-042 | 2026-09-24 | Account deletion and password changes re-check the password through a separate, immediately revoked sign-in (no service-role key). Deletion removes the household's payment records with everything else; PayFast keeps its own transaction record (legal to confirm accounting retention). The deletion confirmation email (US-44 AC2) waits for the email provider (L10 §10); the on-screen confirmation covers it until then. The theme is a per-device cookie read by the server, so there's no script and no flash | Keeps the no-service-role rule; honest about what's still missing | Proposed (owner to confirm retention and email) |
| D-043 | 2026-09-24 | Founding pilot price: **R 50,00 once-off for 90 days**, set by the owner and stored in the hosted `private.plans` (plan open). The payments secret is generated and stored by the owner, never through chat | Owner's decision; keeps the secret out of transcripts | Accepted |
| D-044 | 2026-09-24 | Backups: logical backups with `scripts/db-backup.sh` (Supabase CLI dump) and restores only into a new, empty project with `scripts/db-restore.sh`, which re-applies what the dump leaves out (default privileges, the `auth.users` triggers) and is checked with `scripts/db-acl-snapshot.sql` and `db:test`. Recommended, owner to decide: the Supabase Pro plan (daily backups, no pausing) before the first real user, plus a weekly encrypted off-site copy; PITR only if the pilot grows (`docs/release/operations.md` §1.2) | The rehearsal showed a plain dump-and-restore silently breaks sign-up and account deletion and opens every table to `anon`; the free plan has no backups Supabase keeps | Accepted (procedure); Proposed (plan) |
| D-045 | 2026-09-24 | Pilot metrics come first from the database the app already has (`private.pilot_metrics`, counts only, owner-only, not reachable through the API): sign-ups, setup completion and drop-off, activation, monthly activity, the check-in habit, and conversion. Event analytics (landing, source, clicks, "how this was calculated" and debt-help opens) wait for the owner to choose a privacy-focused tool | Meets the launch gate's activation, repeated-value and conversion measures with no new personal data, cookie or processor | Accepted |
| D-046 | 2026-09-24 | Retention is enforced nightly by `private.purge_expired()` (pg_cron, 01:17 UTC): deleted transactions after 30 days, export link rows after 7 days, audit events after 12 months. The restore re-creates the job (`supabase/restore/after_schema.sql`). Firefox end-to-end runs are a CI job on every PR (WebKit: D-049) | Clears release review risks R3 and R10, and most of R7; the periods follow N11 and how the app uses each table | Accepted (periods confirmed by the owner, D-048) |
| D-047 | 2026-09-24 | Secure session cookies and the CSP's `upgrade-insecure-requests` depend on the site being served over https, not only on the build mode. Env validation refuses an `http://` site URL in production unless the host is localhost | Safari/WebKit rejects Secure cookies and upgrades localhost requests on plain http, which broke the local production build used by the end-to-end tests. Real deployments are https, so they keep both protections, and the staging smoke test checks it | Accepted |
| D-048 | 2026-09-24 | Owner answers to the open questions: trading name **The Ledger Loft & Co** (not formally registered); support through the owner's personal email for now; **one person per account** for the pilot (A-02 confirmed; linked profiles or household groups are a later feature); the owner reviews the legal and safety wording themselves; retention periods in D-046 confirmed; **Supabase Free plan** for now (D-044's Pro recommendation deferred: a weekly `scripts/db-backup.sh` copy is the only backup, and the project can pause after 7 days of low activity); minimal spending on tools; **no Etsy-channel marketing** (the Etsy messaging and mailing-list parts of L10 are dropped; other channels instead); **no PayFast account yet**, so payments stay closed on staging and the owner tests with a one-off test access grant (`docs/release/staging-deploy.md` §2b) | Owner's decisions. The personal email address isn't written in the repo; it goes into the support and privacy pages when they're drafted. A review by the owner isn't the qualified legal review the release review called for under B5, so B5 stays open as an owner-accepted risk | Accepted |
| D-049 | 2026-09-24 | WebKit (Safari's engine) is left out of CI for now; Firefox runs every end-to-end flow on each PR. The `webkit` Playwright project stays available for manual runs. **Open follow-up:** in Playwright's Linux WebKit, `page.goto` crashes the page at `budget.spec.ts:51`, `review-export.spec.ts:93`, `settings.spec.ts:67` and `setup.spec.ts:203`, every run and retry, under both iPhone and Desktop Safari profiles, and also when the network is idle first. The other 46 WebKit tests pass. Fixed on the way: Secure cookies and `upgrade-insecure-requests` on local http (D-047), the theme cookie, the join redirect and the streamed title | The build environment can't run WebKit or read the CI traces, and two fixes failed. The owner chose to merge with Firefox and check Safari on a real iPhone (R7) instead of blocking the PR | Accepted (owner) |
